apiVersion: v1
kind: Service

metadata:
  name: aws-java-app
  namespace: devops-demo

spec:
  type: LoadBalancer

  selector:
    app: aws-java-app

  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP